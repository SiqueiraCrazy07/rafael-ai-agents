# Predictive Runtime Intelligence

Camada preditiva para antecipar riscos operacionais do runtime.

Na V1, os preditores analisam `memory/` e geram forecast em:

- `runtime-data/predictive/`
- `memory/predictive/`
