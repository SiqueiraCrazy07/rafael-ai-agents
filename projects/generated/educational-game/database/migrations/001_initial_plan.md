# Migration Plan

This is a readonly-safe migration plan. It is not executed automatically.

- Create users table with JSON payload and audit timestamps.
- Create progress table with JSON payload and audit timestamps.
- Create content table with JSON payload and audit timestamps.
- Create dashboard_events table with JSON payload and audit timestamps.
- Create missions table with JSON payload and audit timestamps.
- Create scores table with JSON payload and audit timestamps.
- Create lessons table with JSON payload and audit timestamps.
- Create mastery_states table with JSON payload and audit timestamps.
